@model WebUI.Models.UpdatePersonVM

@{
    ViewBag.Title = "UpdatePerson";
}

<h2>UpdatePerson</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Persoon</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.EditorFor(model => model.RequestedPerson.EntiteitId, new { htmlAttributes = new { type = "hidden" } })

        <div class="form-group">
            @Html.LabelFor(model => model.RequestedPerson.FirstName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.RequestedPerson.FirstName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.RequestedPerson.FirstName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.RequestedPerson.LastName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.RequestedPerson.LastName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.RequestedPerson.LastName, "", new { @class = "text-danger" })
            </div>
        </div>

        @foreach (var item in Model.OrganisationChecks.Organisations)
        {
            <div class="checkbox">
                <label>
                    <input type="checkbox"
                           name="SelectedOrganisations"
                           value="@item.Value" /> @item.Text
                    </label>
                </div>
        }

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "ChangePlatform", "Platform", new { id = Model.RequestedPerson.PlatformId }, null)
</div>
